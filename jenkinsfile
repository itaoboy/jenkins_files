pipeline {
    agent any

    def username='jenkins_name'

    stages {
        stage('Build') {
            steps {
                echo 'output---:${username}'
		ech0 'output global--${env.JENKINS_URL}'
		echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
